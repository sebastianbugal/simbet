<!DOCTYPE html>
<html>
<head>
  <title>LeaderBoards</title>
  <link rel="stylesheet" type="text/css" href="/stylesheets/leaderBoard.css">
  <% include ../partials/header.ejs %>
</head>

<body>
  <% if(session.role == ('a' || 'm')) { %>
    <% include ../partials/adminNav.ejs %>
  <% } else { %>
    <%include ../partials/nav.ejs%>
  <%} %>

  <div class="leaderboard_container">
    <table class="chess_leaderboard">
      <tr>
        <th>Rank</th>
        <th>Username</th>
        <th>Wins</th>
        <th>Ties</th>
        <th>Losses</th>
        <th>Elo</th>
      </tr>
      <% var rank = 1; %>
      <% rows.forEach(function(r) { %>
        <tr>
        <td><%= rank %></td>
        <td><%= r.username %></td>
        <td><%= r.wins %></td>
        <td><%= r.ties %></td>
        <td><%= r.losses %></td>
        <td><%= r.chess_elo %></td>
      </tr>
      <% rank++;}); %>

    </table>

  <div class="embedded_twitter">
    <a href="https://twitter.com/intent/tweet?button_hashtag=SplatForum&ref_src=twsrc%5Etfw" class="twitter-hashtag-button" data-show-count="false">Tweet #SplatForum</a>
    <script async src="https://platform.twitter.com/widgets.js" charset="utf-8"></script>
  </div>
</div>

  
</body>
</html>
